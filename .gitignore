# Node modules (should never be committed)
node_modules/

# Build files
dist/
out/

# Lock files (useful for local dependency management, but usually ignored in Git)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Webpack-related cache & output
.cache/
webpack.config.js

# Logs & temporary files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store  # macOS system file
Thumbs.db  # Windows system file


# Editor/IDE files (prevent user-specific settings from being committed)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Miscellaneous
node_modules/
coverage/ # If you're running tests with coverage reports
